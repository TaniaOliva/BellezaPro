# Puerto donde corre la API
PORT=3000
JWT_SECRET=

# Cadena de conexión a MongoDB Atlas (Database > Connect > Drivers)
# Formato: mongodb+srv://<usuario>:<password>@<cluster>.mongodb.net/bellezapro?retryWrites=true&w=majority
MONGODB_URI=

# Credenciales de la cuenta de correo para el envío de notificaciones
EMAIL_USER=
EMAIL_PASS=
